package application;

import java.util.Arrays;
import java.util.IntSummaryStatistics;
import java.util.List;
import java.util.Scanner;

public class funcao {

	public static void main(String[] args) {
		
/*		
	// 	Como conseguir o maior número que existe em uma lista
		
		List numbers = Arrays.asList (3, 2, 2, 3 , 7, 3, 5);

		IntSummaryStatistics stats = integers.stream(). MapToInt((x)-> x).summaryStatistics();

		System.out.println ("Menor número na lista:" + stats.getMin ());

		
	// 	Como obter a segunda sexta-feira do próximo mês

		LocalDate firstInYear=LocalDate.of (date1.getYear (), date1.getMonth (), 1);

		LocalDate secondFriday=firstInYear.with (TemporalAdjusters.nextOrSame (DayOfWeek.FRIDAY)). With (TemporalAdjusters.next (DayOfWeek.FRIDAY));

		System.out.println (“Segunda sexta-feira em:”+ segunda sexta-feira);
		
		// Qual é a diferença entre findFirst () e findAny ()?
		
		findFirst () retorna o primeiro elemento que atende ao critério, enquanto findAny () retorna qualquer elemento que atenda ao padrão
		
	
	//	Como escrever a primeira letra de todas as palavras de uma string 
	  
	 	public class firstLetter {

			public String getFirstLetters(String text) { 
		
				String firstLetters = ""; 
		
				text = text.replaceAll("[.,]", ""); 
				// Replace dots, etc (optional) 
				
				for(String s : text.split(" ")) { 
					firstLetters += s.charAt(0); 
					} 
				
				return firstLetters; 
			}
		}
	  
	 
 	//	Write a program to print
		* * * * *
		* * * *
		* * *
		* *
		*
		* *
		* * *
		* * * *
		* * * * *
		
		with minimum number of variable use
		


		public class StarPrint {
		
			public static void main(String[] args)	throws java.lang.Exception { 
					
				int k = 0;
				
				for(int i=5;i>=1;i--){
					int j = i;
					k++;
					System.out.printf("%"+k+"s"," ");
				
					while(j > 0){
						System.out.print( " * ");
						j--;
					}
					System.out.println();
				}
		
				for(int i=1;i<=5;i++){
					int j = 1;
					System.out.printf("%"+k+"s"," ");
					while(j <= i){
						System.out.print(" * ");
						j++;
					}
					k--;
					System.out.println();
				}
			}
		} 
		
		
		// A program to print the serie: 4,6,12,18,30,42,60,72

		public class NumSer2 {
		
			public static void main(String[] args) {
		
				System.out.println("Enter limit:"); 
				Scanner sc = new Scanner(System.in); 
				int n = sc.nextInt(); 
				int count; 
				
				for(int i=3;i!=n;i++) { 
					count=0; 
					for(int j=2;j<=(i-1)/2;j++) { 
						if((i-1)%j==0) { 
							count++; 
							break; 
						} 
					}
					for(int j=2;j<=(i+1)/2;j++) { 
						if((i+1)%j==0) { 
							count++; 
							break; 
						} 
					} 
					if(count==0) { 
						System.out.println(i); 
					} 
				} 
			} 
		}
	
	
	//	Como encontrar elementos duplicados em um array e imprimir o array removendo as duplicatas
	  
	 	public class NumSer2 {
		
			public static void removeDups(int[] arr, int n) { 
	 		// Hash map will store the elements which has appeared previously. 
	 		
	 		HashMap mp = new HashMap(); 
	 		
	 		for (int i = 0; i &lt; n; ++i) { 
	 			// Print the element if it is not there in the hash map 
	 			 
	 			 if (mp.get(arr[i]) == null) {
	 			 	System.out.print(arr[i] + " "); 
	 			 }// Insert the element in the hash map 
	 			 
	 			 mp.put(arr[i], true); 
 			} 
		}
	  
	  
	 
	// 	Métodos wait(), notify() e notifyAll()
	 	
	 	The wait() method causes the current thread to wait until another thread invokes the notify() or notifyAll() methods for that object. 
	 	The notify() method wakes up a single thread that is waiting on that object's monitor. The notifyAll() method wakes up all threads 
	 	that are waiting on that object's monitor
	 	
	 	
	 	
	 	
*/
		
		
	}

}
